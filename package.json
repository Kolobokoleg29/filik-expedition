{
  "name": "filik-expedition",
  "version": "5.3.4",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npm run test:unit",
    "test:unit": "node --test tests/unit/*.test.mjs",
    "test:syntax": "node scripts/check-syntax.mjs",
    "test:content": "node .agents/skills/filik-content/scripts/validate-content.mjs",
    "test:budget": "node scripts/check-budgets.mjs",
    "test:release": "node scripts/check-release.mjs",
    "release:package": "npm run build && npm run test:budget && npm run test:release && node scripts/package-release.mjs",
    "test:e2e": "npm run build && playwright test",
    "check": "npm run test:syntax && npm run test:content && npm run test:unit && npm run build && npm run test:budget && npm run test:release",
    "check:all": "npm run check && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "vite": "8.2.2"
  }
}