{
  "name": "filik-expedition",
  "version": "5.3.4",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npm run test:unit",
    "test:unit": "node --test tests/unit/*.test.mjs",
    "test:syntax": "node scripts/check-syntax.mjs",
    "test:content": "node .agents/skills/filik-content/scripts/validate-content.mjs && node scripts/check-gameplay-content.mjs && node scripts/check-asset-contracts.mjs",
    "test:product": "node scripts/check-yandex-draft.mjs && node scripts/check-purchases-catalog.mjs",
    "test:budget": "node scripts/check-budgets.mjs",
    "test:release": "node scripts/check-release.mjs",
    "release:package": "npm run test:product && npm run build && npm run test:budget && npm run test:release && node scripts/package-release.mjs",
    "sdk:dev": "npm run build && node scripts/prepare-sdk-dev.mjs && npx --yes @yandex-games/sdk-dev-proxy@0.0.2 -p dist --dev-mode=true --port 8080 --csp",
    "test:e2e": "npm run build && playwright test",
    "check": "npm run test:syntax && npm run test:content && npm run test:product && npm run test:unit && npm run build && npm run test:budget && npm run test:release",
    "check:all": "npm run check && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "vite": "8.2.2"
  }
}
